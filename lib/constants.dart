import 'package:flutter/material.dart';

const Duration defaultPixelDuration = Duration(milliseconds: 200);
const Curve defaultCurve = Curves.easeIn;
BorderRadius defaultBorderRadius = BorderRadius.circular(20);
